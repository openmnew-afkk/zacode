{
  "buildCommand": "cd ZCodeProject && npm install --cache /tmp/npm-cache && npm run build",
  "outputDirectory": "ZCodeProject/dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
